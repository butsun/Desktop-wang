[project]
name = "stockfloat"
version = "0.1.0"
description = "Local floating A-share watchlist desktop widget."
requires-python = ">=3.11"
dependencies = [
  "PySide6>=6.7.0",
  "requests>=2.32.0",
]

[tool.ruff]
line-length = 100
target-version = "py311"
